<%= form_for [@article, Comment.new] do |f| %>
    <div class="form-line">
        <%= f.text_field :content, placeholder: "Comment..." , class: 'form-control' , required: true %>
        <%= f.submit "Comment" , class: "btn btn-primary" %>
    </div>
<% end %>