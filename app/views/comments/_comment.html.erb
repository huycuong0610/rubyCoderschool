<p><%= comment.content %></p>
<p><strong>Author:</strong><%= User.find(comment.user_id).email %></p>
<% if user_signed_in? %>
    <%  if comment.user_id == current_user.id %>        
        <%= link_to 'Edit', edit_article_comment_path(comment.article, comment) , class: "bn btn-success"%>
        <%= link_to 'Delete', article_comment_path(comment.article, comment) , method: :delete, data: {confirm: "Do you want to delete this comment?"} %>
    <% end %>
<% end %>